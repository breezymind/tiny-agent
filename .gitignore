# ─────────────────────────────────────────────
# 민감정보 / 개인정보 (SECRETS — 절대 커밋 금지)
# ─────────────────────────────────────────────
# OAuth 액세스/리프레시 토큰 포함
auth.json
# API 키 지문 및 내부 엔드포인트 포함 (재생성 가능한 캐시)
litellm-models.json

# 자격증명 계열 일반 패턴
*.key
*.pem
*.p12
*.pfx
*.crt
*credentials*
*secret*
.env
.env.*

# ─────────────────────────────────────────────
# 개인정보 (대화 로그 · 로컬 경로 · 사용자명 노출)
# ─────────────────────────────────────────────
# 세션 대화 기록 (개인 프로젝트 경로/사용자명/내용 포함)
sessions/

# Local Pi package fetch/cache state
.pi/
# 로컬 절대경로·사용자명이 박힌 MCP 설정
mcp.json
.claude/settings.local.json

docs/issues.sqlite-wal
docs/issues.sqlite-shm

# ─────────────────────────────────────────────
# 캐시 / 생성물 (재생성 가능)
# ─────────────────────────────────────────────
mcp-cache.json
.auto-index/
.codegraph/
*cache*

# ─────────────────────────────────────────────
# 의존성 / 바이너리 (커밋 대상 아님)
# ─────────────────────────────────────────────
node_modules/
bin/
*.log
**/__pycache__/
*.pyc

# ─────────────────────────────────────────────
# OS / 에디터 잡파일
# ─────────────────────────────────────────────
.DS_Store
**/.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

settings.local.json
.venv/

# Pi runtime state kept in the user's agent home, never in the package source.
.mcp.json
settings.json
models-store.json
trust.json
docs/issues.sqlite
